#include <stdio.h>
#include "main.h"
/**
 * _strcat - function that concatenates two strings.
 * @dest: first entry
 * @src: second entry
 * return 0
 */
char *_strcat(char *dest, char *src)
{
char concat = dest + src;
return (concat);
}
